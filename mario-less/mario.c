#include <cs50.h>
#include <stdio.h>
int main(void)
{
    int width;
    do
    {
            int width = get_int("Width: ");

    }while(width>0;);

    for(int i = 0; i < width; i++)
         {
                    printf("#");
                    for(int j = 0; j < width; j++)
                    {
                        print
                    }

         }
}