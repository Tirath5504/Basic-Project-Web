#include <cs50.h>
#include <stdio.h>

void input(void);

int main(void)
{
    for(int i = width; i > 0; i--)
    {
        input(void);
        for(int j = width; ; j++)
        {
                if(j < )
        }
        printf("\n");
    }
}