#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int width = get_int("Width: ");
    if(width>0)
    {
            for(int i = 0; i < width; i++)
            {
                    printf("#");
                    
    }
    else
        goto 6;
}