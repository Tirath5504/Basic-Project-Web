#include <stdio.h>

int main(void)
{
    int n1, n2, n;
    scanf("%d", &n);
    int num[n];
    char str[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &num[i]);
        scanf("%s", &str[i])
    }
    
    return 0;
}