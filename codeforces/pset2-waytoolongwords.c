#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n;
    string str;
    printf("Enter a number: ");
    scanf("%d", &n);
    for(int i = 1; i <= n; i++)
    {
        scanf("%s", &str);
        
    }
}